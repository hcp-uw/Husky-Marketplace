     /** @type {import('tailwindcss').Config} */
     module.exports = {
      content: [
        './src/**/*.{js,jsx,ts,tsx, css}', // Note the addition of 'ts' and 'tsx' if using TypeScript
      ],
      theme: {
        extend: {},
      },
      plugins: [],
    }